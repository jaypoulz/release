chain:
  as: upi-libvirt-heterogeneous-post
  steps:
  - chain: gather
  - ref: upi-libvirt-deprovision
  - ref: upi-libvirt-cleanup-post
  documentation: |-
    The UPI cleanup step contains all steps that gather and deprovision an Openshift cluster on multi-arch.